rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /empresas/{empresa}/anexos/{file} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.resource.size < 20 * 1024 * 1024; // 20MB
    }
  }
}
